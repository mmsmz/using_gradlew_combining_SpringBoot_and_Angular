plugins {
  id 'java'
  id("com.github.node-gradle.node") version "2.2.0"
}

node{
  version= "14.15.0"
  npmVersion = "6.14.8"
  download = true
}

jar.dependsOn 'npm_run_build'

jar{
  from 'dist/vbook-ui' into 'static'
}
